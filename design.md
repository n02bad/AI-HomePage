# 首页个性化设计方案

## 项目定位

首页个性化不是单纯换皮，而是让管理员用 AI 输入首页目标，再通过模块化配置生成适合不同运营场景的用户端首页。最终首页应该像一个可组合的业务工作台：不同券商、不同客户阶段、不同运营目标，可以复用相同模块，但用不同顺序、样式和显隐策略组合。

## AI 默认视觉优先规则

- 后续 AI 生成首页默认必须遵守“宽松白底金融 SaaS”视觉基线，除非管理员明确要求暗色终端、黑金 VIP、活动海报或其他强品牌模式。
- 页面必须以白色或极浅蓝灰为主，主体居中，桌面最大宽度控制在 1400px 到 1500px，左右留白保持宽松。
- 排版必须优先保证 24px 到 32px 的模块节奏、24px 到 32px 的卡片内边距、稳定左对齐和清晰纵向阅读顺序。
- 主色必须克制使用金融蓝，只用于主按钮、选中态、关键标签和重要数字；不要把整页铺成浅青、深色、炫光或大面积渐变。
- 首页首屏必须只突出 1 到 2 个最高视觉重点，其他信息通过灰蓝说明、小标签、轻边框和低对比度向下展开。

## AI 应用方式

这份文档必须作为 AI 页面生成的设计治理文件使用。AI 可以写漂亮的 HTML，也可以对积木块做审美升级，但漂亮必须建立在 ForexCRM / Nxbroker 金融 CRM 的产品气质、组件语义和数据契约上。

生成前必须先判断：

- 用户端首页的主意图：资产管理、开户引导、专业交易、活动增长、CopyTrading、IB/代理推广或默认工作台。
- 页面应该使用哪些白名单模块，而不是自由发明业务能力。
- 哪些内容来自后台配置或接口，哪些只是预览占位。
- 是否需要 AI HTML、组件化蓝图、骨架 HTML 或积木组件。

生成中必须遵守：

- 先用规范锁住骨架，再允许 AI 在模块比例、信息层级、状态细节、图表/列表表达、空状态和响应式上做美化。
- 美观不能来自随机渐变、厚重阴影、大圆角、营销式 hero、英文装饰标签或卡片堆叠。
- 颜色优先使用首页主题 token，例如 `--home-bg`、`--home-card-bg`、`--home-primary`、`--home-text`、`--home-border`、`--home-radius-sm`。
- AI HTML 类名必须使用 `ai-html-` 前缀，CSS 必须限制在当前 HTML 草稿范围内。
- 系统动作必须通过 `data-home-action` 标记，不能编造不存在的动作。
- 真实交易账号、资金、PnL、保证金、图表、活动、FAQ、客服、风险披露、APP 下载等数据必须来自后台或接口；预览缺失时使用 `--`、Sample 或明确的后台绑定说明。

生成后必须自检：

- 是否符合当前主意图，而不是只换颜色。
- 是否使用 allowedBlocks 和组件库语义。
- 是否没有旧模块、禁用模块或白名单外业务。
- 是否没有营销页式大封面、卡片套卡片、硬编码颜色过多、装饰渐变过多。
- 是否支持白天/黑夜主题和移动端单列降级。
- 是否有明确的主视觉焦点、主操作、数据层级、状态表达和空/加载/缺省处理。

一句话原则：漂亮不是自由发挥更多，而是在 CRM 规范内把信息层级、模块组合和交互状态做得更专业。

## 默认视觉规范：宽松白底金融 SaaS

这套规范是后续 AI 生成用户端首页的默认审美基线。它学习的是白底金融 SaaS 首页的空间、排版和层级，不固定任何组件形态，也不要求照搬某张参考图的模块顺序。

### 页面画布

- 页面必须以白色或极浅蓝灰为主，背景应该接近 `#ffffff` 到 `#f8fbff`，只允许非常轻的蓝灰层次。
- 页面主体必须居中，桌面端建议最大宽度为 1400px 到 1500px；宽屏不要让内容无限拉伸。
- 桌面端左右留白应该保持 64px 到 96px；移动端左右留白应该保持 20px 到 24px。
- 首屏内容不能贴住页面顶部，欢迎信息、核心资产或主任务之前应该有明确的顶部呼吸感。
- 不要使用大面积青色、紫色、深色、炫光、彩色渐变或整页浅色块作为默认背景。

### 排版与留白

- 页面必须采用清晰的纵向阅读节奏，主要区块之间建议保持 24px 到 32px 间距。
- 区块内部 padding 应该保持 24px 到 32px；紧凑工具条可以降低，但不能让内容贴边。
- 同一行元素之间建议保持 12px 到 20px 间距；按钮、卡片、输入区域需要稳定对齐。
- 所有主要内容的左边缘应该尽量对齐，形成可扫描的网格秩序。
- 避免多层卡片嵌套、边框套边框和模块内再堆很多小白卡。
- 如果模块很多，优先用留白、弱化文字和分组节奏降噪，不要单纯缩小字号或压缩间距。

### 信息层级

- 首页首屏必须只保留 1 到 2 个最高视觉重点，例如核心资产、开户下一步、账户表现或关键操作。
- 重要金额、账号状态或关键指标必须使用更大的数字和更强字重，成为视觉锚点。
- 次要说明、标签和辅助统计应该使用灰蓝色、小字号和低对比度。
- 不要让每个模块都使用高对比标题、强背景、强按钮或大数字；首页需要主次分明。
- 首页应该有概览感和可信赖感，不要做成运营后台报表页，也不要做成营销落地页。

### 字体层级

- 页面主标题建议 28px 到 36px，字重 700 到 800。
- 区块标题建议 18px 到 22px，字重 700。
- 卡片标题建议 13px 到 15px，字重 600 到 700。
- 说明文字建议 13px 到 14px，颜色使用 `#64748b` 或相近灰蓝色。
- 重要金额和关键数字建议 28px 到 40px，字重 800 到 900，并使用 tabular number 或金融数字风格。
- 字体颜色必须以深蓝黑为主，例如 `#0f172a`、`#111827`、`#172033`。

### 卡片、按钮与颜色

- 卡片必须以纯白为主，使用浅蓝灰细边框，例如 `#dbeafe`、`#dce6f4`、`#e2e8f0`。
- 卡片阴影必须非常轻，只用于增强层次；避免厚重投影和悬浮海报感。
- 卡片圆角建议 8px 到 12px，最多不超过 14px；不要用过大的胶囊圆角做普通卡片。
- 主色优先使用金融科技蓝，例如 `#2563eb`；蓝色只用于主按钮、选中态、关键标签和重点数字。
- 成功、警告、错误等功能色只能小面积使用，不能改变整页主基调。
- 主按钮应使用蓝底白字，次按钮应使用白底、浅边框和蓝色或深色文字；按钮高度建议 40px 到 48px。
- 图标可以增强操作感，但必须简洁、线性、低装饰感，不能让图标比信息本身更抢眼。

### 版式气质

- 页面应该像高端金融产品的客户端首页：安静、宽松、可信赖、数据清楚。
- 首屏应该先建立欢迎、资产、下一步或关键任务的秩序，再让次要模块向下展开。
- 账号、产品、任务、FAQ、风险提示等模块可以选择不同表现形式，但都必须遵守同一套留白、边框、按钮和文字层级。
- 表格适合大量明细；首页核心内容优先使用可扫描的卡片、指标带、轻量列表或分组布局。
- 生成时不要固定组件结构，也不要复制参考图内容；只学习白底、宽松排版、浅边框卡片、轻阴影、蓝色强调、大数字层级和低信息密度。

## 页面结构

### 1. AI 输入页

路径：`home-layout-admin.html`

用途：让管理员描述首页思路，并获得建议方案。

页面内容：

- AI 输入框：输入品牌调性、首屏重点、客户类型、模块顺序、想强化或弱化的功能。
- 建议方案：根据输入生成 4 个候选方向。
- 生成预览：保存草稿并进入预览页。
- 恢复默认首页：清空已发布配置。

不放在这里的内容：

- 完整首页 iframe。
- 批注模式。
- 默认展开的 JSON 配置。
- 大量随机/优化按钮。

### 2. 首页预览页

路径：`home-layout-preview.html`

用途：查看首页内容区域，并调整模块组合与模块样式。

页面内容：

- 方案摘要：布局、主题、密度、首屏重点。
- 栏目拼接：展示每个栏目由哪些模块组成。
- 模块样式：每个模块提供几个可选样式。
- 预览 iframe：只展示首页内容区域，不展示共用导航和顶部搜索。
- 发布到首页：把草稿保存为正式首页配置。

### 3. 用户端首页

路径：`client-home.html`

用途：用户看到的真实首页。

要求：

- 使用已发布配置。
- 不展示预览控制。
- 左侧导航、顶部搜索、页签继续来自共用布局。

### 4. 首页积木组件库

路径：`home-module-preview.html`

用途：预览首页积木模块和每个模块的样式方向。

页面内容：

- 模块分类：核心模块、增强模块、表格/表单模块。
- 组件预览：每个模块展示 1 到 3 个代表样式。
- 尺寸标记：展示模块适合的 `1x1`、`1x2`、`2x1`、`2x2`、`3x1`、`3x2` 规格。
- 首页细组件：补齐 `client-home.html` 里的欢迎头、资金按钮、账号筛选、视图切换、邀请字段等细颗粒组件。
- AI 组件工作台：调用大模型生成组件定义，保存到组件库，再基于保存组件生成首页草稿。

## 模块设计

模块积木的完整拆解见 `home-module-bricks.md`。后续每个模块扩展样式时，以这份拆解里的模块 ID、尺寸和业务职责为基础。

### 广告轮播图

后台配置：

- 是否开启。
- 广告内容、链接、排序。
- 在首页中的位置：首屏、活动区域、辅助区域。

样式建议：

- 沉浸大图：适合品牌和活动首屏。
- 清爽轮播：适合专业交易首页。
- 短轮播：适合放在资产或快捷入口旁边。

设计原则：

- 如果关闭广告轮播，首页结构应自动回收空间。
- 广告不要抢走入金、开户、交易账号这些高频操作的可见性。

### 快捷入口

后台配置：

- 数量：3、4、5、6、7、8。
- 每个入口的 icon、文案、链接、排序。
- 展示方式：icon+文案、仅 icon、hover 显示文案。

样式建议：

- 快捷矩阵：适合功能完整的默认首页。
- 工具条：适合专业交易工作台。
- 紧凑网格：适合信息密度高的首页。

设计原则：

- 入口数量超过 6 时，应更偏工具条或紧凑网格。
- 仅 icon 样式必须有 hover/focus 文案或 tooltip。

### 钱包模块

后台配置：

- 是否开启。
- 单独展示或聚合到资产模块。
- 是否展示入金/出金按钮。
- 展示币种、折算币种、汇率说明。

样式建议：

- 钱包卡片：单独模块展示钱包余额。
- 钱包摘要：作为资产模块里的一个指标。
- 钱包操作条：余额和入金/出金并列。

设计原则：

- 如果钱包模块关闭，但资产模块开启，总资产仍可包含钱包资产。
- 入金/出金按钮出现时，要像完整操作按钮，不像小文字链接。

### 资产模块

后台配置：

- 是否开启。
- 展示总资产、钱包资产、交易账号资产。
- 是否展示入金/出金按钮。
- 是否突出为首屏主模块。

样式建议：

- 资产驾驶舱：大数字 + 分项资产。
- 指标条：适合横向拼接。
- 轻量卡片：适合下方辅助区域。

设计原则：

- 总资产 = 钱包资产 + 交易账号资产。
- 如果钱包模块单独展示，资产模块也可以只展示总资产和交易账号资产，避免重复。

### 邀请链接模块

后台配置：

- 是否展示链接打开数。
- 是否展示邀请注册数。
- 是否展示交易账号开通数。
- 推广链接、邀请码、二维码是否展示。

核心信息：

- 推广链接。
- 邀请码。
- 二维码。

样式建议：

- 邀请控制台：数据和复制入口完整展示。
- 链接优先：先展示推广链接和复制按钮。
- 紧凑邀请：只保留关键指标和复制入口。

设计原则：

- 统计数据可隐藏，但推广链接、邀请码、二维码属于重要信息，默认应该保留至少一种可复制/可打开能力。

### 交易账号模块

后台配置：

- 真实账号是否展示。
- 模拟账号是否展示。
- 真实和模拟账号合并展示或分开展示。
- 展示方式：表格、列表、卡片、账户卡墙、真实/模拟分组、可切换工作台。

样式建议：

- 账号工作台：筛选 + 开户入口 + 卡片/列表切换。
- 紧凑卡片或账户卡墙：适合账号较少、需要突出某个账户或活动资格的场景。
- 安静列表或运营长表：适合专业交易客户、账号较多或字段较多的场景。

设计原则：

- 真实账号和模拟账号可以一起展示，也可以拆成两个栏目。
- 如果展示切换按钮，切换控件要轻，不要成为页面重点。
- 账号模块应该和开户功能靠近，因为它们属于同一业务路径。
- 不应默认把交易账号套成白色大卡片；生成逻辑应根据账号数量、字段密度和用户操作目的选择版式。

### 开户模块

后台配置：

- 开真实账号是否展示。
- 开模拟账号是否展示。
- 绑定交易账号是否展示。
- 展示位置：交易账号模块内、首屏快捷入口、开户路径模块。

设计原则：

- 默认与交易账号模块结合。
- 三个动作应该是明确按钮：开真实账号、开模拟账号、绑定交易账号。
- 如果后台只开启其中一个或两个，布局要自动收缩，不留空位。

## 推荐首页组合

### 默认平衡首页

适合大多数用户。

栏目顺序：

1. 资产模块 + 入金/出金
2. 快捷入口
3. 广告轮播图
4. 交易账号模块 + 开户按钮
5. 邀请链接模块

特点：功能完整，学习成本低。

### 高净值/VIP 首页

适合高净值客户、品牌型券商。

栏目顺序：

1. 资产模块作为首屏驾驶舱
2. 入金/出金按钮
3. 广告轮播图或权益 Banner
4. 交易账号模块
5. 邀请链接模块弱化

特点：留白更大、资产更突出、操作入口更少但更清晰。

模块样式建议：钱包操作条、资金双按钮、横向开户、快捷工具条、安静列表。

### 活动增长首页

适合活动、交易大赛、入金转化。

栏目顺序：

1. 广告轮播图
2. 入金按钮 + 快捷入口
3. 活动/邀请模块
4. 交易账号模块
5. 资产模块

特点：转化路径强，适合短期运营。

模块样式建议：沉浸轮播、资金双按钮、快捷紧凑网格、资产指标条、赛事看板、链接优先邀请。

### 专业交易工作台

适合活跃交易客户。

栏目顺序：

1. 资产摘要 + 快捷工具条
2. 交易账号模块
3. 入金/出金
4. 广告轮播图弱化
5. 邀请链接模块

特点：信息密度更高，交易账号优先。

模块样式建议：资产指标条、资金紧凑行、快捷工具条、账号安静列表、广告短轮播。

### 新客开户首页

适合新用户、未完成 KYC 或首次入金用户。

栏目顺序：

1. 开户路径
2. 开真实账号 / 开模拟账号 / 绑定交易账号
3. 入金入口
4. 资产和钱包摘要
5. 快捷入口
6. 广告轮播图

特点：下一步清晰，减少新用户迷路。

模块样式建议：开户清单、横向开户、资金双按钮、轻量资产卡、邀请链接优先。

## 后台配置模型建议

后台应把首页配置拆成三层。

### 页面级配置

```js
{
  enabled: true,
  theme: "classic | aurum | ocean | energy",
  density: "compact | balanced | spacious",
  layout: "classicStack | executiveHero | conversionStack | traderConsole | partnerGrowth",
  heroFocus: "assets | adCarousel | tradingAccounts | onboarding"
}
```

### 栏目级配置

```js
{
  sections: [
    {
      id: "hero",
      type: "hero | split | rail | full",
      title: "首屏",
      slots: ["assets", "fundActions", "adCarousel"]
    }
  ]
}
```

### 模块级配置

```js
{
  modules: {
    adCarousel: { enabled: true, style: "immersive" },
    quickActions: { enabled: true, count: 6, style: "iconText" },
    wallet: { enabled: true, placement: "mergedWithAssets", showFundActions: false },
    assets: { enabled: true, showFundActions: true },
    referral: { enabled: true, showClicks: true, showRegistrations: true, showTradingAccounts: false },
    tradingAccounts: { enabled: true, accountTypes: ["real", "demo"], grouping: "combined", viewMode: "switchable" },
    openAccount: { enabled: true, real: true, demo: true, bind: true, placement: "insideTradingAccounts" }
  }
}
```

## 当前项目优化方向

### 第一阶段：把静态原型稳定成模块系统

- 已补齐 `moduleSettings`，让开启/关闭、入口数量、交易账号展示方式进入配置层。
- 已把钱包模块从资产展示中拆出可独立渲染能力，同时保留聚合到资产模块的配置。
- 已把开户按钮作为交易账号模块的可配置子能力，支持开真实账号、开模拟账号、绑定账号显隐。
- 已保留草稿/发布机制，预览页调整不会直接影响真实首页。

### 第二阶段：增强预览页，但保持简单

- 页面风格预设已经从“只改布局/主题”升级为“布局 + 主题 + 模块样式 + 关键配置”一起切换。
- 模块配置面板已经支持“启用/关闭”开关。
- 快捷入口已经支持数量选择和展示方式选择。
- 交易账号模块已经支持“真实/模拟合并或分开”。
- 资产和钱包已经支持“聚合/拆分”。
- JSON 配置继续放在 details 中，不作为默认主操作。

### 第三阶段：接后台配置

- 从本地 `localStorage` 切换为后台接口。
- 后台保存草稿、发布版本、回滚版本。
- 广告、快捷入口、邀请链接、二维码数据从后台配置项读取。
- 用户端首页只读取已发布版本。

### 第四阶段：AI 方案能力

- AI 输入页输出标准化蓝图。
- AI 不直接修改共用导航和顶部搜索。
- AI 只能选择模块、顺序、样式、显隐、强调项。
- AI 输出必须通过配置校验，避免丢失关键业务能力。

## 成功标准

- 管理员能用一句话生成首页预览。
- 管理员能看懂首页由哪些栏目和模块拼成。
- 管理员能为每个模块选择少量清晰样式。
- 发布前不会影响真实用户首页。
- 首页模块能覆盖广告、快捷入口、钱包、资产、邀请、交易账号、开户七类核心功能。
- 共用导航、顶部搜索、页签不会被误当成首页配置项。
